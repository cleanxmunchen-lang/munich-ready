import { requireAdmin } from '@/lib/admin-auth';
import QRCode from 'qrcode'; import { supabaseAdmin } from '@/lib/supabase'; import { formatPrice } from '@/data/catalog';
export default async function Hotels() { await requireAdmin(); const { data: hotels } = supabaseAdmin ? await supabaseAdmin.from('hotels').select('id,name,ref_code,commission_percent,active,orders(subtotal,payment_status,hotel_commission)') : { data: [] }; const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000'; return <><p className="eyebrow">Partners</p><h1 className="mt-2 text-3xl font-bold">Hotels</h1><div className="mt-6 grid gap-5 md:grid-cols-2">{await Promise.all((hotels ?? []).map(async (hotel: any) => { const paid = (hotel.orders ?? []).filter((order: any) => order.payment_status === 'paid'); const sales = paid.reduce((sum: number, order: any) => sum + order.subtotal, 0); const commission = paid.reduce((sum: number, order: any) => sum + (order.hotel_commission ?? 0), 0); const url = `${baseUrl}/?ref=${hotel.ref_code}`; const qr = await QRCode.toDataURL(url, { width: 180, margin: 1 }); return <article className="card p-5" key={hotel.id}><div className="flex gap-5"><img src={qr} width="120" height="120" alt={`QR code for ${hotel.name}`} /><div><h2 className="font-bold">{hotel.name}</h2><p className="mt-1 text-sm text-black/60">Ref: {hotel.ref_code} · {hotel.active ? 'Active' : 'Inactive'}</p><p className="mt-3 text-sm">Commission: {hotel.commission_percent}%<br/>Paid orders: {paid.length}<br/>Attributed sales: {formatPrice(sales)}<br/>Estimated owed: {formatPrice(commission)}</p></div></div><a className="mt-4 block break-all text-sm text-[#184f3a] underline" href={url}>{url}</a></article>; }))}</div>{!hotels?.length && <p className="mt-6 text-black/60">No hotel partners yet, or Supabase is not configured.</p>}</>; }
