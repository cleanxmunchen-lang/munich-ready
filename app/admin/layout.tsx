import Link from 'next/link'; import { cookies } from 'next/headers'; import { redirect } from 'next/navigation';
export default async function AdminLayout({ children }: { children: React.ReactNode }) { if ((await cookies()).get('munich_ready_admin')?.value !== process.env.ADMIN_PASSWORD) redirect('/admin/login'); return <><header className="border-b border-black/10 bg-white"><div className="shell flex h-16 items-center gap-5"><Link href="/admin/orders" className="font-black">MUNICH READY <span className="text-[#184f3a]">ADMIN</span></Link><nav className="ml-auto flex gap-4 text-sm font-semibold"><Link href="/admin/orders">Orders</Link><Link href="/admin/hotels">Hotels</Link></nav></div></header><main className="shell py-8">{children}</main></>; }
